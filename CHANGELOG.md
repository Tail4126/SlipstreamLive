# 変更履歴

**Slipstream Live** の変更点をまとめる。

書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/)、バージョン番号は [セマンティック バージョニング](https://semver.org/lang/ja/) に準拠。

---

## [未リリース]

## [1.1.3] - 2026-08-17

### 追加

- アンインストール時に、任意回答のアンケートフォームを新しいタブで開くようにした。`runtime.setUninstallURL()` へ URL を登録するだけの `background.js`（Chrome ではサービスワーカー、Firefox ではイベントページ）を新設。登録内容はブラウザ側に保持されるため、削除後に自前のコードが動く必要はない。
  - 拡張機能自身は何も送信しない。URL は全ユーザー共通の固定値で、識別子・設定・視聴内容は一切付与しない。
  - これに伴い `PRIVACY.md` と `docs/privacy-policy.html` に「アンインストール時のアンケート」の節を追加し、最終更新日を 2026-08-17 に更新。`README.md` / `README.ja.md` のプライバシー節も「動作中は通信なし」という表現へ改めた。

### 修正

- 自動調整が「オフ」のとき、バッファ残量が「加速するバッファ閾値」を超えているのに加速しないことがある不具合を修正。原因は 2 つあった。ひとつはヒステリシス（0.2 秒）を入口側へ足していたことで、実際の判定線が 入口＝閾値＋0.2 秒 ／ 出口＝閾値 となり、ユーザーの設定値が「加速をやめる線」として働いていた。もうひとつは、モードを切り替えてから 2 秒は加速を開始しない最小滞在時間（`DWELL_MS`）が手動モードにも掛かっていたことである。
- 上記の対策として、手動モード（段階 0）ではヒステリシスと最小滞在時間の両方を掛けないようにした。どちらも谷の統計 `room` が 0.5 秒幅で揺れることへの対策であり、ユーザーが決めた 1 本の線と生の観測値を比べるだけの手動モードには必要がない。ヒステリシスは入口へ足せば設定値が「加速をやめる線」になり、出口から引けば「加速をやめる線」が設定値より 0.2 秒低くなる。どちらへ倒してもユーザーが指定した 1 つの数字が 2 本の線に分裂するため、掛けないことにした。これにより手動モードは「閾値に達したら加速、下回ったら戻る」という設定どおりの動きになる。のこぎり波 1 周期ごとに倍率が切り替わる場面は増えるが、予測しやすさを優先した。0.15 倍のブレーキ（`floor`）は別の判定なので、そのヒステリシスは手動モードでも従来どおり効く。「標準」「積極的」の挙動は変更していない。
- 「バッファ枯渇時に最低速度にする」がオフのとき、確保する下限が段階ごとの上乗せ量（0.1〜0.5 秒）だけになっていた問題を修正。0.15 倍の緊急ブレーキが無い状態では谷が 0 を割った瞬間に映像が止まるため、ブレーキを切ったほうが下限が低いという逆転が起きていた。自動調整が「標準」「積極的」の場合、下限を `adapter.needs()`（サイトが返すセグメント長の目安。YouTube 1〜5 秒 / Twitch 2 または 4 秒 / ツイキャス 0.5 秒）へ置き換え、セグメント 1 個ぶんを確保するようにした。自動調整が「オフ」のときの動作は変わらない。

### 変更

- 日本語 UI の表記を修正。「バッファ枯渇時に最低速度する」→「バッファ枯渇時に最低速度にする」、「最低速度する閾値（秒）」→「最低速度にする閾値（秒）」。`README.ja.md` と取扱説明書（日本語）の同じ表記もあわせて修正した。
- 自動調整の 3 段階の表に、下限の決まり方とヒステリシス・最小滞在時間の掛かり方についての注記を追加（README 英日・取扱説明書 英日）。

## [1.1.2] - 2026-08-15

### 修正

- 配信の最先端に追いついた後も加速を続け、映像が数秒おきに一瞬止まるだけで速度が上がらなくなる不具合を修正。ライブ映像は実時間でしか作られないため、先読みの貯金を使い切った時点で詰められる遅れは存在しなくなる。そこで倍率を保つと再生がセグメントの到着を追い越して細かく停止し、実効速度は 1.0 倍（ときにそれ以下）へ落ちる。バッジには 1.25x と表示されているのに等倍にしか感じられない、という報告の原因はこれ。
- 上記の対策として、加速の**実効性を実測で検証する仕組み**（`Gain`）を追加した。「加速で詰められるはずだった秒数」（$\int (rate-1)dt$）と「実際に詰められた秒数」（$\Delta$再生位置 − $\Delta$実時間）を時間窓で突き合わせ、前者が 1 秒を超え、かつ 1.5 秒以上観測したうえで、後者が前者の半分に満たなければ、バッファ残量に関係なく加速を見送る。この状態はバッファ残量では検出できない（停止のたびに読み込みが追いつくため残量は 2〜4 秒で安定し、`room` は下限を上回りつづける）ので、残量ではなく結果を直接見る必要があった。
  - Auto の `drift` 積算と異なり、この測定はデータ待ちで停止している時間を**あえて含める**。止まっている時間こそが失われている量そのものであり、除外すると損失が観測できなくなるため。除外するのは一時停止とシークのみ。
  - 時間窓は既定倍率で 12 秒。判定に必要な要求量を貯められるよう倍率が低いほど伸ばし、設定の下限である 1.05 倍では 30 秒になる。窓を固定すると低い倍率で判定が永久に下りなくなるため。
  - 見送りは、遅延が `adapter.needs() × 0.25`（下限 0.20 秒・上限 1.00 秒）ぶん増えたとき、または 30 秒経過したときに解除される。定常時の遅延はサイトとブラウザで 12 倍も差があり（Chrome/Twitch 5.05 秒、Firefox/ツイキャス 0.79 秒、Chrome/ツイキャス 0.40 秒）、固定の秒数では意味が揃わない。`adapter.needs()` はサイトが返すセグメント長の目安であり、そのサイトにおける遅延の自然な単位そのものなので、これを基準にすると遅延に対する感度がサイト間で揃う。
  - 下限 0.20 秒は実測の遅延ノイズ上限（全 6 環境で標準偏差 ≤ 0.11）のおよそ 2 倍。実際にリバッファが起きた瞬間の変化量は 0.81 秒と 0.93 秒で、ノイズとは明確に分離している。ノイズとは別に、実イベントを伴わない数十秒周期のゆるやかな揺らぎ（Twitch で振幅 0.35〜0.36 秒）があり、全環境でしきい値がこの上に来ることを確認した。
- 制御モードが 0.5〜1 Hz で往復し、そのたびに `playbackRate` が書き換わって映像が細かく途切れる不具合を修正。判定に使う `room` は実測で 0.5 秒幅の揺れを持つため、ヒステリシス（従来 0.05〜0.1 秒）では境界での往復を止めきれなかった。ヒステリシスを全段階 **0.2 秒**へ広げたうえで、**加速を始める遷移（Normal → Speedup）だけ**に 2 秒の最小滞在時間を課した。介入を強める方向だけを慎重にし、弱める方向・安全側へ戻る方向（加速からの離脱、Floor の出入り）は待たせない。
  - Speedup → Normal を待たせると、危ないと判断した後も加速を続けて枯渇を自分で招く。Floor からの脱出を待たせると、既定値では脱出条件を満たしてから 1.5 秒ぶん余計に 0.15 倍で走り、追いつくための機能が遅れを増やす結果になる。
- 制御モードが切り替わった時点で谷の履歴を破棄するようにした。モードが変わるとバッファの水準そのものが動くため、長期窓の中に遷移前後の 2 つの水準が同居すると `troughSd` がばらつきではなく水準の移動量を測ってしまい、`room = troughAvg - troughSd × K` が大きく沈む。これが長期窓の長さと同じ周期の自励振動を生んでいた。
  - ただし Normal → Speedup では破棄しない。加速中の水準低下は自分の倍率が原因で `drift` 補償が打ち消すため履歴が汚れず（実測でも加速中の `troughSd` は 0.2 前後に収まり、破綻していたのは補償の対象外である脱出後の再充填の側だけだった）、破棄すると直後に `room` が `NaN` へ戻って加速に入った次の tick で降りる空回りになるため。
- YouTube と Twitch で、長い広告のあと制御が復帰しなくなる不具合を修正。休止時にタイマーを完全停止させ、復帰の合図をメディアイベントに頼っていたことが原因。両サイトとも広告を本編と同じ `<video>` で再生するため、広告が終わっても要素の再ロードも一時停止も起きず、`loadstart` / `play` / `playing` のいずれも発火しない。変わるのは `ad-showing` クラスや広告 UI といった DOM の状態だけで、これは待ち受けようが無い。休止中もタイマーを 1 秒周期で回し続けるようにして、DOM の変化を自力で拾うようにした。
  - 見張りの費用は小さい。休止中の tick は設定属性の文字列比較と `adapter.video()` / `adapter.media()` だけで終わり、注入先も `manifest.json` の `matches` により対象サイトのフレームに限られる（広告の iframe は別オリジンなので入らない）。
  - アダプタ側は無変更。「広告明けを DOM で検知する」といった個別の仕掛けを増やさずに済んでいる。

### 変更

- ヒステリシス幅が段階ごとに異なる仕様をやめ、全段階で 0.2 秒に統一。往復の抑制は最小滞在時間が担うため、段階ごとに変える意味が無くなったため。`AUTO_TUNING` の段階表からも取り出し、独立した定数 `HYSTERESIS` にした。
- デバッグログに `gain=` を追加。「実際に詰められた秒数 / 詰められるはずだった秒数」を表示し、加速を見送っている間は `FUTILE` が付く。
- デバッグログに `lat=` を追加。サイトが報告する遅延の平均・標準偏差・報告間隔あたりの最大変化量を出す。見送りの自動解除しきい値は遅延の差分と比較するため、その値のばらつきを確認できるようにした。
- `adapter.status()`（遅延の取得）を毎 tick 呼び、実効性の検証・遅延ノイズの計測・バッジ表示で結果を共有するようにした。従来はバッジ描画のときだけ呼んでいた。
- 制御状態と統計の後始末を `purge()`（統計だけを捨てる）と `restart()`（制御状態も含めて初期化する）へ集約した。従来は同じ数行が配信切り替え時・休止時・シーク時の 3 か所に散らばっており、状態を 1 つ増やすたびに 3 か所へ手を入れる必要があった。
- `settle()` に `force` 引数を追加し、ユーザーがプレイヤー UI で速度を選んだときの離脱を専用の分岐ではなく `settle('normal', now, true)` で表現するようにした。
- タイマーを完全停止するのは機能を OFF にしたときだけにした。この場合の復帰の合図は `<html data-slpstrm>` の書き換えに限られ、`MutationObserver` が確実に拾うため、取りこぼしが起こらない。
- 上記に伴い、`sleep()` の引数を「確信できるか」を表す真偽値から休止中の周期（ミリ秒）へ変更し、探索の猶予（`GRACE_MS` / `seekAt`）を削除した。
- `wake()` の役割を「停止中のループを叩き起こす」から「見張り周期の待ちを潰して tick を前倒しで 1 回走らせる」へ変更。`visibilitychange` の分岐も同じ扱いに統一した。
- 休止しても `<video>` を掴んだままにした（完全停止するときのみ手放す）。メディアイベントの購読が続くため、タイマーが間引かれる裏タブでの追従が良くなる。
- デバッグログの `timer stopped` は機能 OFF 時にのみ出るようになった。従来は正常時も頻出しており異常との区別が付かなかったため、README・取扱説明書・Issue テンプレートの記述をあわせて更新した。
- YouTube の「最低速度する閾値」の既定値を Chrome・Firefox とも 0.80 秒に統一（Firefox は 1.00 秒から引き下げ）。加速の実効性検証（`Gain`）の追加以降、Firefox でも挙動が安定したため。
- ツイキャスの「最低速度する閾値」の既定値を Chrome・Firefox とも 0.30 秒に統一（Chrome は 0.10 秒から引き上げ）。0.10 秒では反応が間に合わずストールすることがあったため。
- 上記により、Firefox 用の既定値の上書き（`ff`）が残るのは Twitch のみになった。
- Twitch の閾値についての注記を、Chrome と Firefox で値が違う理由まで含めた内容へ書き換えた（9 言語）。Chrome は途切れたままエラー #3000 で停止して再読み込みが要るのに対し、Firefox は読み込み待ちになるだけで自動復帰する。

## [1.1.1] - 2026-08-14

### 修正

- YouTube で広告再生中に加速と最低速度のあいだで速度が激しく往復する不具合を修正。`getVideoData()` は広告中も本編の `video_id` と `isLive` を返し続けるため `media()` が広告を検知できず、広告のバッファ（尺のぶん丸ごと読み込み済み → 末尾で枯渇）を本編の指標として読んでしまっていた。プレイヤーの `ad-showing` クラスで広告区間を判定し、広告中は `id: 'ad'` / `live: false` を返すことで `inject.js` 側が動画の切り替わりとして扱うよう修正（`ad-created` は広告終了後も残り続けるため未使用）。

## [1.1.0] - 2026-08-14

### 追加

- **取扱説明書**を追加。`docs/manual.html`（英語）と `docs/manual.ja.html`（日本語）の2ページ構成で、インストール・設定項目の一覧・バッジの見かた・トラブルシューティングを画面写真つきで解説する。図版26枚は `docs/images/` に配置。
- 設定ポップアップの右下に、取扱説明書へのリンクを追加。UI 言語が日本語のときは日本語版、それ以外の8言語では英語版が開く。
- `_locales/*/messages.json`（9言語）にキー `manual`（リンクの文言）と `manualUrl`（リンク先 URL）を追加。
- `docs/index.html` のヘッダーナビと、フッターの「ドキュメント」欄に取扱説明書へのリンクを追加。
- `docs/style.css` を新設。GitHub Pages の4ページが共有する唯一のスタイルシート。
- Firefox アドオン（addons.mozilla.org）での公開に伴い、`README.md` / `README.ja.md` の Firefox インストール手順にストアページへの直接リンクを追加。従来の一時的な読み込み・署名無効化の手順は、開発者向けとして `<details>` 内に移動。
- Android 版 Firefox（142.0 以降）にも同じストアページから導入できる旨を README および GitHub Pages に明記。
- `docs/index.html` のヒーローに「Firefox に追加」ボタン、フッターの「プロダクト」に Firefox アドオンへのリンクを追加。

### 変更

- Chrome ウェブストアへのリンクを、スラッグを含む URL から拡張機能 ID のみの正規 URL へ統一（計5箇所）。ストア掲載名を変更してもリンクが切れないようにするため。
- ストアへのリンクはロケールを含まない形（`addons.mozilla.org/firefox/addon/...`）で統一。AMO が閲覧者の言語設定に応じて自動でリダイレクトするため、英語版・日本語版の双方から同じ URL を参照できる。
- `docs/index.html` のインストール手順で、Firefox タブを一時的な読み込み手順からストア経由の手順へ差し替え。ソースからの手動読み込みは補足として残置。
- `docs/` の4ページ（`index.html` / `manual.html` / `manual.ja.html` / `privacy-policy.html`）に個別に書かれていた CSS を `docs/style.css` へ統合。デザイン変数・ヘッダー・表・コード・図版・注記・フッターを共通化し、ページ固有のルールは `<body>` の `page-home` / `page-doc` / `page-manual` / `page-policy` クラスで切り分ける。ビルド工程は追加していない。
- 上記の統合に伴う見た目の調整。本文幅をプライバシーポリシーだけ 840px だったものを 900px へ、本文行間を 1.75 と 1.8 の混在から 1.8 へ、ヘッダー高さを 68px と 64px の混在から 64px へ統一。プライバシーポリシーのヘッダーも他ページと同じ追従（sticky）表示にした。
- リンク色の既定を全ページで `--floor` に統一。トップページのインストール手順にあったリンクが本文と同色で判別できなかった問題が解消される。
- `popup.js` の翻訳適用処理に `data-href` 属性のサポートを追加。要素の `href` を `_locales` の文字列で差し替えられるようになり、言語ごとのリンク先の出し分けをコード側の分岐なしで表現できる。
- `PRIVACY.md` と `docs/privacy-policy.html` に、設定画面から外部ページ（GitHub Pages）へのリンクがある旨を明記。クリックするまで何も読み込まないこと、リンクに識別子を付けないことを併せて記載し、最終更新日を 2026-08-14 に更新。
- `README.md` / `README.ja.md` の冒頭と「使い方」、`CONTRIBUTING.md`、`SECURITY.md` に取扱説明書への導線と対象範囲の記述を追加。
- シーク完了（`seeked`）を検知したら、バッファ残量の統計を破棄して作り直すようにした。シークは残量を不連続に飛ばすため、短期窓の中で古い標本と新しい標本が1本の系列として混ざると、平均・標準偏差・傾きのいずれも意味を失うため。
- デバッグログに `ample=` を追加。谷の統計を待たずに加速してよいと判断する残量（秒）を表示する。あわせて `auto=` の位置を `ample=` の直後へ移動。

### 修正

- 手動でシークした直後など、バッファの読み込みが急に速くなる場面で定常判定（`calm`）が通らなくなり、残量が数十秒あっても加速せず等倍のままになる問題を修正。先読みが一気に進むと取り込み速度が跳ね上がり、逆に追っかけ再生でブラウザ側のバッファ上限に達すると取得が止まるため、補償座標の傾きが 0.9 秒/秒 の範囲から外れたままになる。すると谷の履歴が貯まらず `room` が `NaN` のままになり、加速の判断ができなくなっていた。
- 上記の対策として、残量が 20 秒（「最低速度する閾値」を大きくしている場合は「確保したいバッファ量＋10 秒」）を超えている間は谷の統計を迂回して加速し、15 秒を下回ると従来の判定へ戻るようにした。谷の推定は「のこぎり波の谷が下限を割らないか」を精密に見極めるための仕組みであり、残量がその下限より桁違いに多い場面では判断材料として不要になるため。この近道は自動調整が「標準」「積極的」のときだけ働き、「オフ」（手動閾値）の動作は変わらない。

## [1.0.3] - 2026-08-10

### 追加

- 設定ポップアップのヘッダーに、拡張機能アイコンからプロジェクトのホームページ（GitHub Pages）を新しいタブで開くリンクを追加。

### 変更

- 谷（trough）の履歴が `room` の算出に使われるまでに必要な最短時間（`TROUGH_MIN_MS`）を3秒から**1秒**に短縮。再生開始直後や配信の頭出し直後でも、より早くバッファ推定が有効になる。

## [1.0.2] - 2026-08-10

### 追加

- Chrome ウェブストア（Chrome Web Store）への公開に伴い、`README.md` および `README.ja.md` のインストール手順にストアページへの直接リンクを追加。

### 変更

- Firefox の対応バージョン下限を 128.0 から **140.0** へ、Firefox for Android を **142.0** へ引き上げ。`manifest.json` で宣言している `browser_specific_settings.gecko.data_collection_permissions` は Firefox 140 / Firefox for Android 142 で追加されたキーで、それ以前のバージョンでは解釈されないため（addons.mozilla.org の検証で警告として指摘される）。Chromium 系の下限（`minimum_chrome_version: 128`）は変更なし。
- 上記に伴い README.md / README.ja.md の「対応環境」、CONTRIBUTING.md の前提条件を更新。

---

## [1.0.1] - 2026-08-08

### 修正

- 各言語のメッセージファイル（`_locales/de/messages.json`、`_locales/en/messages.json`、`_locales/es/messages.json`、`_locales/fr/messages.json`、`_locales/pt_BR/messages.json`）にて、長かったテキスト・メッセージ文言を短縮・最適化。

---

## [1.0.0] - 2026-08-08

初回公開リリース。

### 追加

**再生制御**

- 通常 / 加速 / 最減速の3モードで自動制御。20ミリ秒周期のタイマーに加え、`timeupdate` / `progress` / `waiting` イベントでも判定するため、裏タブでタイマーが間引かれても制御は止まらない。
- **加速（Speedup）**：バッファに余裕があると自動的に加速（既定1.25x）し、ライブ最先端へ追いつく。
- **最減速（Floor）**：バッファ枯渇直前に0.15xまで落とし、完全停止（くるくる）を避けつつバッファを急速回復させる。安全装置なので速度は固定値。
- **音量ダッキング**：最減速中だけ音量を一時的に絞る（既定：現在の音量の30%）。極端なスロー再生時の音声の歪みを抑え、通常速度に戻れば自動で元の音量に戻る。
- 状態境界にヒステリシスを設け、しきい値ちょうどで倍率・音量が細かく往復するのを防止。幅は自動しきい値の段階に連動し、オフ・標準では0.1秒、積極的では0.05秒。
- 制御対象が見つからないときは1秒周期の探索モードへ。5秒経っても確定しなければタイマーを止めてメディアイベント待ちに移行し、`visibilitychange` でも復帰する。

**バッファしきい値の自動推定**

- のこぎり波モデルに基づく谷（trough）の統計的推定。短期の平均と標準偏差から `trough = avg − √3 × sd` を算出するため、配信のセグメント長を知らなくても計算できる。
- 推定した谷を長期窓に蓄積し、実効余裕 `room = troughAvg − 安全余裕係数 × troughSd` を算出。この値が「確保したいバッファ下限」を上回る間だけ加速する。
- 自身の速度変更で統計を汚さないよう補正。1.0倍からの超過消費 `D(t) = ∫(rate − 1)dt` を累積し、各標本を補償座標 `health + D(t)` で保持する。「加速→余裕が枯れる→通常速度に戻る→また加速」という自励振動が起きない。
- 配信が定常かどうかを判定。補償座標の傾き（＝取り込み速度−1）が0.9秒/秒を超えて動いている間は谷を推定せず、谷の履歴も破棄する。起動直後やスタール復帰直後の観測がその後の統計を汚す問題を防ぐ。判定自体も水準の履歴が1秒分貯まるまで保留。
- 統計の標本は等間隔のタイマー駆動tickからのみ採取。`waiting`などバッファ枯渇時に集中して発火するイベント駆動tickでは超過消費の積算のみ行う。のこぎり波を一様分布とみなす前提が崩れるのを防ぐため。
- 3段階のスライダー（オフ / 標準 / 積極的）。段階ごとに谷を貯める長期窓の長さ・安全余裕係数・確保したい下限への上乗せ量・ヒステリシスを切り替え。標準は30秒窓×係数5×上乗せ0.3秒、積極的は5秒窓×係数3×上乗せ0.1秒。
- 短期窓はアダプタごとの目安値 `needs()` を基準に自動調整し、1〜30秒の範囲に制限。
- 標本8個以上・窓の50%以上・谷の履歴3秒分が揃うまでは推定を保留。データ不足時は `room` が `NaN` になり比較が常にfalseになるため、自動的に「何もしない」に倒れる。
- 一時停止中・シーク中・データ待ち（`readyState < 3`）の間は超過消費の積算を見送る。再生位置が進まない区間まで「倍率分消費した」と数えると補償が過剰になり、谷の推定が狂うため。

**サイト対応**

- YouTube（公式プレイヤーAPI `#movie_player` を利用）、Twitch（React fiber探索でプレイヤーコアを取得）、ツイキャスの3サイト向けアダプタ。
- 制御対象はライブ再生のみ。VOD・クリップ・広告再生中には一切介入しない。
- ツイキャスの低遅延配信は制御対象外。低遅延モードはHLSではなくWebRTCで映像が届くため読み先バッファが存在せず、`playbackRate` も仕様上無視されるため制御が成立しない。`media()` でMediaStreamを検出して対象外にし、タイマーごと停止する。低遅延をオフにすればメディアイベント経由で自動復帰。
- サイト別の既定値を用意。Twitchは約10秒に及ぶ映像データの途切れに備え最減速しきい値を2.00秒に設定、ツイキャスはバッファが本質的に浅いため0.10秒に設定。
- Firefoxはバッファ残量の報告のされ方が異なるため、専用の既定しきい値を用意。
- バッファの隙間の許容値をサイト別に設定（YouTube・ツイキャス0.5秒、Twitch2秒）。広告明けなどの不連続を正しく扱う。

**再生権の制御**

- `hijack` 機構を実装。サイトから見える値と実際に効いている値を分離することで、サイト側プレイヤーとの速度の奪い合いを防ぎ、ダッキングでユーザーの音量設定が恒久的に書き換わる事態も回避。
- YouTube・ツイキャスでは、ユーザーがプレイヤーのメニューで1.00x以外の速度を選んだ場合は制御を譲る。Twitchには速度変更UIがないため常に制御を維持し、あわせてプラットフォーム側の追いつき機能（`setLiveSpeedUpRate`）を1.0に固定して競合を防ぐ。

**インターフェース**

- 設定ポップアップ（オプションページ兼用）。サイト別タブを備え、現在開いているサイトのタブを自動選択。
- プレイヤー上の任意バッジ3種（既定はすべてオフ）。再生速度倍率（白＝通常/赤＝加速中/青＝最減速中）、遅延秒数（巻き戻し視聴中は `(DVR)`）、バッファ残量（隙間の先にも読み込み済み映像がある場合は `+3s` を併記）。再描画は100ミリ秒間隔（毎秒最大10回）に制限。
- コントロールバーを検出できないプレイヤー向けに、左上に出すフォールバック用の帯UI。
- セクション単位のリセットボタンと、全設定項目に付く `?` ヘルプ。
- 9言語のUI（日本語・英語・韓国語・ドイツ語・スペイン語・フランス語・ポルトガル語(BR)・簡体中文・繁体中文）。

**設計**

- Manifest V3対応。Chrome 128以降/Firefox 128.0以降。
- 設定の型・範囲・刻み幅・既定値は `shared/schema.js` に一元化。
- MAIN world側に独立したフェイルセーフ検証を実装。`data-` 属性が改ざんされても範囲外の値が制御ループに流入しない。
- `window.__slipstreamliveDebug = true` でデバッグ出力（1秒ごとに現在のモード、実再生速度、バッファ残量、各種統計、`room` の計算式、超過消費の累積 `drift`、定常判定 `calm` を表示）。
- 設定画面を `popup.html?locale=xx` で開けば、ブラウザの言語設定を変えずに任意の言語で表示を確認できる（開発用）。

### セキュリティ

- 外部通信、外部コードの読み込み・実行、アクセス解析、テレメトリはいずれも行わない。
- 要求する権限は `storage` と `activeTab` の2つのみ。設定は同期されないローカルストレージに保存。詳細は [PRIVACY.md](PRIVACY.md) を参照。
- 内部ヘルパーは読み込み直後に `globalThis` から削除し、ページ側のスクリプトから参照できないようにしている。

---

## メンテナ向けメモ

<details>
<summary>追記のしかた</summary>

### 1. 変更の記録

変更は `## [未リリース]` の下に、次の見出しのいずれかで追記する。

| 見出し | 用途 |
| :--- | :--- |
| `追加` | 新機能 |
| `変更` | 既存動作の変更 |
| `非推奨` | 将来削除予定の機能 |
| `削除` | 削除された機能 |
| `修正` | 不具合修正 |
| `セキュリティ` | 脆弱性・プライバシー関連の変更 |

過去のリリース見出しの内容は当時の事実の記録なので、後から書き換えない。実装が変わった場合は `未リリース` 側へ差分として追記する。

### 2. リリース時

`## [未リリース]` を `## [x.y.z] - YYYY-MM-DD` に変更し、新しい空の `未リリース` セクションを作る。あわせて次の2つも更新する。

- `manifest.json` の `version` を最新の見出しと一致させる
- このファイル末尾のリンク定義に新しい版を追加し、`未リリース` の比較範囲を差し替える

### 3. バージョン番号の上げ方

- **メジャー（x.0.0）** — 設定項目の削除・改名、体感できる既定値の変更、対応ブラウザ要件の引き上げ。
- **マイナー（0.x.0）** — 新機能、対応サイトの追加、設定項目の追加、対応言語の追加。
- **パッチ（0.0.x）** — 不具合修正、文言・翻訳の修正、既存動作を回復させるためのセレクタ更新。

### 4. 関連ファイルの同期

保存内容や使用権限に影響する変更をした場合は、同じコミットで [PRIVACY.md](PRIVACY.md) の本文と冒頭の「最終更新」日、および `docs/privacy-policy.html` も更新する。設定項目や既定値を変更した場合は、README.md / README.ja.md の設定一覧表、`docs/manual.html` / `docs/manual.ja.html` の設定一覧表、`_locales/` 配下の説明文の更新も必要。

制御モードの追加・削除やバッジ色の変更は次の5か所すべてに現れる。片方だけ直すと食い違うので、必ずまとめて更新すること。

1. `inject.js` の `COLOR` と制御状態
2. `_locales/*/messages.json` の `showPlaybackRateDesc`
3. README.md / README.ja.md の制御モード表
4. `docs/manual.html` / `docs/manual.ja.html` の制御モード表（バッジ色の見本 `.dot` も含む）
5. この CHANGELOG

設定画面の見た目を変えた場合は、取扱説明書の図版も撮り直す。図版は `docs/images/Fig_<番号>[-<言語>].png` の命名で、英日それぞれ1枚ずつ用意する。番号は本文中の図番号と一致させ、差し替え時もファイル名は変えない。

</details>

[未リリース]: https://github.com/Tail4126/SlipstreamLive/compare/v1.1.3...HEAD
[1.1.3]: https://github.com/Tail4126/SlipstreamLive/compare/v1.1.2...v1.1.3
[1.1.2]: https://github.com/Tail4126/SlipstreamLive/compare/v1.1.1...v1.1.2
[1.1.1]: https://github.com/Tail4126/SlipstreamLive/compare/v1.1.0...v1.1.1
[1.1.0]: https://github.com/Tail4126/SlipstreamLive/compare/v1.0.3...v1.1.0
[1.0.3]: https://github.com/Tail4126/SlipstreamLive/compare/v1.0.2...v1.0.3
[1.0.2]: https://github.com/Tail4126/SlipstreamLive/compare/v1.0.1...v1.0.2
[1.0.1]: https://github.com/Tail4126/SlipstreamLive/compare/v1.0.0...v1.0.1
[1.0.0]: https://github.com/Tail4126/SlipstreamLive/releases/tag/v1.0.0
