# 変更履歴

**Slipstream Live** の変更点をまとめる。

書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/)、バージョン番号は [セマンティック バージョニング](https://semver.org/lang/ja/) に準拠。

---

## [未リリース]

## [1.0.2] - 2026-08-10

### 追加

- Chrome ウェブストア（Chrome Web Store）への公開に伴い、`README.md` および `README.ja.md` のインストール手順にストアページへの直接リンクを追加。

### 変更

- Firefox の対応バージョン下限を 128.0 から **140.0** へ、Firefox for Android を **142.0** へ引き上げ。`manifest.json` で宣言している `browser_specific_settings.gecko.data_collection_permissions` は Firefox 140 / Firefox for Android 142 で追加されたキーで、それ以前のバージョンでは解釈されないため（addons.mozilla.org の検証で警告として指摘される）。Chromium 系の下限（`minimum_chrome_version: 128`）は変更なし。
- 上記に伴い README.md / README.ja.md の「対応環境」、CONTRIBUTING.md の前提条件を更新。

---

## [1.0.1] - 2026-08-08

### 修正

- 各言語のメッセージファイル（`_locales/de/messages.json`、`_locales/en/messages.json`、`_locales/es/messages.json`、`_locales/fr/messages.json`、`_locales/pt_BR/messages.json`）にて、長かったテキスト・メッセージ文言を短縮・最適化。

---

## [1.0.0] - 2026-08-08

初回公開リリース。

### 追加

**再生制御**

- 通常 / 加速 / 最減速の3モードで自動制御。20ミリ秒周期のタイマーに加え、`timeupdate` / `progress` / `waiting` イベントでも判定するため、裏タブでタイマーが間引かれても制御は止まらない。
- **加速（Speedup）**：バッファに余裕があると自動的に加速（既定1.25x）し、ライブ最先端へ追いつく。
- **最減速（Floor）**：バッファ枯渇直前に0.15xまで落とし、完全停止（くるくる）を避けつつバッファを急速回復させる。安全装置なので速度は固定値。
- **音量ダッキング**：最減速中だけ音量を一時的に絞る（既定：現在の音量の30%）。極端なスロー再生時の音声の歪みを抑え、通常速度に戻れば自動で元の音量に戻る。
- 状態境界にヒステリシスを設け、しきい値ちょうどで倍率・音量が細かく往復するのを防止。幅は自動しきい値の段階に連動し、オフ・標準では0.1秒、積極的では0.05秒。
- 制御対象が見つからないときは1秒周期の探索モードへ。5秒経っても確定しなければタイマーを止めてメディアイベント待ちに移行し、`visibilitychange` でも復帰する。

**バッファしきい値の自動推定**

- のこぎり波モデルに基づく谷（trough）の統計的推定。短期の平均と標準偏差から `trough = avg − √3 × sd` を算出するため、配信のセグメント長を知らなくても計算できる。
- 推定した谷を長期窓に蓄積し、実効余裕 `room = troughAvg − 安全余裕係数 × troughSd` を算出。この値が「確保したいバッファ下限」を上回る間だけ加速する。
- 自身の速度変更で統計を汚さないよう補正。1.0倍からの超過消費 `D(t) = ∫(rate − 1)dt` を累積し、各標本を補償座標 `health + D(t)` で保持する。「加速→余裕が枯れる→通常速度に戻る→また加速」という自励振動が起きない。
- 配信が定常かどうかを判定。補償座標の傾き（＝取り込み速度−1）が0.9秒/秒を超えて動いている間は谷を推定せず、谷の履歴も破棄する。起動直後やスタール復帰直後の観測がその後の統計を汚す問題を防ぐ。判定自体も水準の履歴が1秒分貯まるまで保留。
- 統計の標本は等間隔のタイマー駆動tickからのみ採取。`waiting`などバッファ枯渇時に集中して発火するイベント駆動tickでは超過消費の積算のみ行う。のこぎり波を一様分布とみなす前提が崩れるのを防ぐため。
- 3段階のスライダー（オフ / 標準 / 積極的）。段階ごとに谷を貯める長期窓の長さ・安全余裕係数・確保したい下限への上乗せ量・ヒステリシスを切り替え。標準は30秒窓×係数5×上乗せ0.3秒、積極的は5秒窓×係数3×上乗せ0.1秒。
- 短期窓はアダプタごとの目安値 `needs()` を基準に自動調整し、1〜30秒の範囲に制限。
- 標本8個以上・窓の50%以上・谷の履歴3秒分が揃うまでは推定を保留。データ不足時は `room` が `NaN` になり比較が常にfalseになるため、自動的に「何もしない」に倒れる。
- 一時停止中・シーク中・データ待ち（`readyState < 3`）の間は超過消費の積算を見送る。再生位置が進まない区間まで「倍率分消費した」と数えると補償が過剰になり、谷の推定が狂うため。

**サイト対応**

- YouTube（公式プレイヤーAPI `#movie_player` を利用）、Twitch（React fiber探索でプレイヤーコアを取得）、ツイキャスの3サイト向けアダプタ。
- 制御対象はライブ再生のみ。VOD・クリップ・広告再生中には一切介入しない。
- ツイキャスの低遅延配信は制御対象外。低遅延モードはHLSではなくWebRTCで映像が届くため読み先バッファが存在せず、`playbackRate` も仕様上無視されるため制御が成立しない。`media()` でMediaStreamを検出して対象外にし、タイマーごと停止する。低遅延をオフにすればメディアイベント経由で自動復帰。
- サイト別の既定値を用意。Twitchは約10秒に及ぶ映像データの途切れに備え最減速しきい値を2.00秒に設定、ツイキャスはバッファが本質的に浅いため0.10秒に設定。
- Firefoxはバッファ残量の報告のされ方が異なるため、専用の既定しきい値を用意。
- バッファの隙間の許容値をサイト別に設定（YouTube・ツイキャス0.5秒、Twitch2秒）。広告明けなどの不連続を正しく扱う。

**再生権の制御**

- `hijack` 機構を実装。サイトから見える値と実際に効いている値を分離することで、サイト側プレイヤーとの速度の奪い合いを防ぎ、ダッキングでユーザーの音量設定が恒久的に書き換わる事態も回避。
- YouTube・ツイキャスでは、ユーザーがプレイヤーのメニューで1.00x以外の速度を選んだ場合は制御を譲る。Twitchには速度変更UIがないため常に制御を維持し、あわせてプラットフォーム側の追いつき機能（`setLiveSpeedUpRate`）を1.0に固定して競合を防ぐ。

**インターフェース**

- 設定ポップアップ（オプションページ兼用）。サイト別タブを備え、現在開いているサイトのタブを自動選択。
- プレイヤー上の任意バッジ3種（既定はすべてオフ）。再生速度倍率（白＝通常/赤＝加速中/青＝最減速中）、遅延秒数（巻き戻し視聴中は `(DVR)`）、バッファ残量（隙間の先にも読み込み済み映像がある場合は `+3s` を併記）。再描画は100ミリ秒間隔（毎秒最大10回）に制限。
- コントロールバーを検出できないプレイヤー向けに、左上に出すフォールバック用の帯UI。
- セクション単位のリセットボタンと、全設定項目に付く `?` ヘルプ。
- 9言語のUI（日本語・英語・韓国語・ドイツ語・スペイン語・フランス語・ポルトガル語(BR)・簡体中文・繁体中文）。

**設計**

- Manifest V3対応。Chrome 128以降/Firefox 128.0以降。
- 設定の型・範囲・刻み幅・既定値は `shared/schema.js` に一元化。
- MAIN world側に独立したフェイルセーフ検証を実装。`data-` 属性が改ざんされても範囲外の値が制御ループに流入しない。
- `window.__slipstreamliveDebug = true` でデバッグ出力（1秒ごとに現在のモード、実再生速度、バッファ残量、各種統計、`room` の計算式、超過消費の累積 `drift`、定常判定 `calm` を表示）。
- 設定画面を `popup.html?locale=xx` で開けば、ブラウザの言語設定を変えずに任意の言語で表示を確認できる（開発用）。

### セキュリティ

- 外部通信、外部コードの読み込み・実行、アクセス解析、テレメトリはいずれも行わない。
- 要求する権限は `storage` と `activeTab` の2つのみ。設定は同期されないローカルストレージに保存。詳細は [PRIVACY.md](PRIVACY.md) を参照。
- 内部ヘルパーは読み込み直後に `globalThis` から削除し、ページ側のスクリプトから参照できないようにしている。

---

## メンテナ向けメモ

<details>
<summary>追記のしかた</summary>

### 1. 変更の記録

変更は `## [未リリース]` の下に、次の見出しのいずれかで追記する。

| 見出し | 用途 |
| :--- | :--- |
| `追加` | 新機能 |
| `変更` | 既存動作の変更 |
| `非推奨` | 将来削除予定の機能 |
| `削除` | 削除された機能 |
| `修正` | 不具合修正 |
| `セキュリティ` | 脆弱性・プライバシー関連の変更 |

過去のリリース見出しの内容は当時の事実の記録なので、後から書き換えない。実装が変わった場合は `未リリース` 側へ差分として追記する。

### 2. リリース時

`## [未リリース]` を `## [x.y.z] - YYYY-MM-DD` に変更し、新しい空の `未リリース` セクションを作る。あわせて次の2つも更新する。

- `manifest.json` の `version` を最新の見出しと一致させる
- このファイル末尾のリンク定義に新しい版を追加し、`未リリース` の比較範囲を差し替える

### 3. バージョン番号の上げ方

- **メジャー（x.0.0）** — 設定項目の削除・改名、体感できる既定値の変更、対応ブラウザ要件の引き上げ。
- **マイナー（0.x.0）** — 新機能、対応サイトの追加、設定項目の追加、対応言語の追加。
- **パッチ（0.0.x）** — 不具合修正、文言・翻訳の修正、既存動作を回復させるためのセレクタ更新。

### 4. 関連ファイルの同期

保存内容や使用権限に影響する変更をした場合は、同じコミットで [PRIVACY.md](PRIVACY.md) の本文と冒頭の「最終更新」日も更新する。設定項目や既定値を変更した場合は、README.md / README.ja.md の設定一覧表と `_locales/` 配下の説明文の更新も必要。

制御モードの追加・削除やバッジ色の変更は次の4か所すべてに現れる。片方だけ直すと食い違うので、必ずまとめて更新すること。

1. `inject.js` の `COLOR` と制御状態
2. `_locales/*/messages.json` の `showPlaybackRateDesc`
3. README.md / README.ja.md の制御モード表
4. この CHANGELOG

</details>

[未リリース]: https://github.com/Tail4126/SlipstreamLive/compare/v1.0.2...HEAD
[1.0.2]: https://github.com/Tail4126/SlipstreamLive/compare/v1.0.1...v1.0.2
[1.0.1]: https://github.com/Tail4126/SlipstreamLive/compare/v1.0.0...v1.0.1
[1.0.0]: https://github.com/Tail4126/SlipstreamLive/releases/tag/v1.0.0
