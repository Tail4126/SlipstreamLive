# デバッグログの取得と見かた

## 有効化する方法

1. **拡張機能のコンソールではなく**、配信ページ自体で `F12` を押してブラウザのコンソールを開く
2. 以下を実行する

```js
window.__slipstreamliveDebug = true;
```

3. 症状が出ている状態で30秒ほど待ち、出力をコピーする

これで1秒ごとに現在の内部状態がコンソールへ出力されるようになります（`[slipstreamlive]` プレフィックス付き）。

> **ページ読み込み時のログについて**
> `adapter` 行と `cannot hijack …: accessor not found` 行は、ページを開いた直後（コンソールを開くより前）に流れます。このフラグはページ側の `window` に置かれるため再読み込みで消えてしまい、上の手順では拾えません。通常のデバッグでは気にしなくて構いません。

---

## メインのログ行（1秒ごと）

実際の出力はこのような1行です（YouTube・自動調整「標準」・下限 ON の例）。

```
speedup  rate=1.25 now=8.32+0.00 health2.0s(avg=7.84 sd=1.20 n= 100) trough30.0s(avg=6.50s sd=0.80s n=  12 span=28.4s) room=6.50-5*0.80=2.50s/1.10s ample=20.00s auto=2 drift=0.35s calm=yes gain=1.80/2.10s lat=1.52s(sd=0.08 jump=0.05 n= 250)
```

### 項目ごとの意味

| 項目 | 意味 |
| :--- | :--- |
| 先頭の単語（例 `speedup`） | 現在の制御状態。`normal` / `speedup` / `floor` のいずれか |
| `rate=` | 実際に効いている再生速度（ユーザーがプレイヤーUIで変えた場合や乗っ取り失敗も反映した「本当の値」） |
| `now=A+B` | `A` が現在のバッファ残量（health、途切れず再生できる秒数）、`B` が隙間の先にある未再生バッファ（ahead） |
| `health…s(avg= sd= n=)` | 短期統計窓における健全性の平均・標準偏差・標本数。窓の長さはサイトが報告するセグメント長の目安に追従し、`1.0s`〜`30.0s` の範囲で自動調整される |
| `trough…s(avg= sd= n= span=)` | のこぎり波の「谷」を推定するための長期窓。平均・標準偏差・標本数・実際に貯まっている期間の長さ。窓の長さは `auto=` の段階で決まる（下表） |
| `room=avg-troughK*sd=結果/しきい値` | `room`（実効余裕）の計算式そのもの。`troughAvg − 安全係数×troughSd` で求め、これが右側のしきい値（`margin`）を上回っている間だけ加速する。式中の係数がそのまま `troughK` なので、段階の確認にも使える |
| `ample=` | 谷の推定を待たずに加速してよい「近道」の残量ライン。`max(20, margin + 10)` なので**必ず20秒以上**になる。降りるときは5秒ぶん低い値を使う。効くのは `auto` が1以上のときだけ |
| `auto=` | 自動しきい値スライダーの段階（下表） |
| `drift=` | 自分自身の速度変更による超過消費の累積。統計を汚さないための補正値 |
| `calm=yes/NO` | 配信が定常（傾きが安定）と判定されているか。`NO` の間は谷の履歴が捨てられるため、`room` も `----` になる |
| `gain=got/asked` | 「実際に詰められた秒数 / 詰められるはずだった秒数」。右の値が1秒以上・観測1.5秒以上あるのに左が半分未満だと末尾に `FUTILE` が付き、加速が見送られている状態を示す |
| `lat=avg(sd= jump= n=)` | サイトが報告する遅延の平均・5秒窓での標準偏差・直前1秒での最大変化量・標本数 |

### `auto=` の段階と、それに対応する値

| `auto=` | 段階 | `trough` 窓 | `troughK` |
| :---: | :--- | :---: | :---: |
| `0` | オフ（手動しきい値） | `60.0s` | `10` |
| `1` | 安定 | `60.0s` | `10` |
| `2` | 標準（既定値） | `30.0s` | `5` |
| `3` | 積極的 | `5.0s` | `3` |

`auto=0` のときは `room` も `ample` も判断に使われません（手動しきい値と生の残量を直接比べるだけ）。表示は出続けますが読み飛ばして構いません。

### `margin`（`room=…/` の右側）が何から決まるか

| 条件 | `margin` の値 |
| :--- | :--- |
| 下限機能が ON | `最低速度にする閾値` ＋ 段階ごとの上乗せ（安定 1.0 / 標準 0.3 / 積極的 0.1 秒） |
| 下限機能が OFF、かつ `auto` が1以上 | セグメント1個ぶん（サイトが報告する目安、0.5〜5秒程度） |

下限が OFF のときに上乗せではなくセグメント長を使うのは、谷が0を割っても受け止める 0.15x のブレーキが無いためです。

---

## 読み方のポイント

- **`----` と表示される値**：NaN、つまり取得できていない状態。多くはブラウザが「バッファが空」と報告しているケース
- **`room=-----5*----=----s/1.10s` のように見えるとき**：`room` の3つの値がすべて NaN のときの表示です。ハイフンが繋がって読みにくいだけで異常ではありません。`troughAvg` が `----` なら谷の履歴がまだ無い、ということです
- **`calm=NO` が続く**：定常でないと判定されているだけで異常ではない。ただし谷の履歴は毎回捨てられるため `room` は `----` のまま。なお残量が `ample`（20秒以上）を超えていればこの状態でも加速はする
- **`lat=` の目安**：定常時は `sd` が0.11以下、`jump` が0.2未満に収まるのが正常（全6環境での実測値）。`jump` がこれを大きく超え続ける場合、遅延測定のノイズが大きく、見送りからの復帰（`speedup re-armed`）が意図せず頻発している可能性がある。復帰のしきい値はサイトのセグメント長の1/4で、0.20〜1.00秒の範囲に収められる
- **`gain=` に `FUTILE` が付く**：ライブ最先端に追いついていて、これ以上加速しても実際には進めていないことを検出した状態。README上の「追いついたら加速しない」仕様が正しく働いている証拠。遅延が広がるか、30秒経つと自動的に解除される

---

## 1秒ごとのログ以外に出るイベント行

| ログ内容 | 出るタイミング／意味 |
| :--- | :--- |
| `settings {...}` | `data-slpstrm` 属性経由で設定の変化を検出した時。現在有効な設定一式を表示 |
| `media {...}` | 再生中のメディア（動画ID）が切り替わった時。`live` フラグを含み、YouTube のみ `premiere` フラグも含む |
| `stall {...}` | ライブ再生中にバッファ枯渇（`waiting` イベント）が発生した時。サイト名・再生位置・`readyState`・`health` を記録 |
| `speedup is futile; standing down {...}` | `FUTILE` 状態に入った瞬間。要求量・実績・判定に使った窓の長さを添える |
| `speedup re-armed { recover }` | `FUTILE` で見送っていた加速を再開した時。`recover` はその時に使った復帰しきい値（秒） |
| `cannot hijack playbackRate <error>` | `<video>` の乗っ取りをブラウザに拒まれた時。この要素については所有権を主張せず、速度制御を諦める |
| `badges on fallback shelf <element>` | プレイヤーのコントロールバーが見つからず、バッジを左上の暗い帯へ逃がした時 |
| `tick failed <error>` | メインループが例外を投げた時（サイト側の仕様変更などが原因になりうる） |
| `timer <ms>ms` / `timer stopped` | 監視周期が変わった時。**`timer stopped` は機能をOFFにしたことを意味し、再生中に現れることは想定されていない** — もし再生中にこれが出ていたら不具合の手がかりになる |
| `adapter <name> <url>` | ページ読み込み時に、どのサイト用アダプタが適用されたか（前述のとおり通常は見えません） |

---

## 症状別の切り分け

| 症状 | まず見るところ |
| :--- | :--- |
| まったく加速しない | `auto=` の段階と `room=…/…` の左右。左が右を超えていなければ「まだ安全でない」と判断している状態。`calm=NO` が続いていないかも確認 |
| 最低速度（0.15x）に入りすぎる | `trough` の `sd` が大きくないか。大きければ谷が暴れており、`auto=` を1段階「安定」寄りへ |
| 加速するがすぐ戻る | `gain=` に `FUTILE` が付いていないか。付いていれば仕様どおりの動作 |
| YouTube のプレミア公開で何も起きない | `media {... premiere: true}` の直後に `timer 1000ms` が出ていれば、設定で対象外にしているため意図的に手を引いた状態。`YouTube` タブの「プレミア公開も制御する」を確認 |
| 再生中に `timer stopped` が出る | 想定外。Issue での報告に役立つ手がかり |

Issueで不具合報告する際も、この `timer stopped` が再生中に出ていないか、`gain=` に `FUTILE` が付いているか、`calm` の状態がどうかを見ると原因の切り分けがしやすくなります。